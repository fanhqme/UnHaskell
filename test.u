(import* str)
(import* io)
(run
	(fin (open "2.bin" 0))
	(c (getCharF fin))
	(_ (putStrLn (itoa c)))
	;(fout (open "2.bin" 1))
	;(_ (putCharF fout 255))
	;(_ (close fout))
	;(line readLine)
	;(_ (putStrLn line))
	;(r (system "ls -l"))
	;(_ (putStrLn (itoa r)))

	;(_ (putStrLn (join " " (splitStr '.' "ha.wa.pa.la"))))
	;(_ (putStrLn (join " " (list "ba" "haha" "wawa"))))
	;(args getArgs)
	;(_ (putStrLn (join " " args)))

#|
	(fin (open "1.txt" 0))
	(a (readIntF fin))
	(b (readIntF fin))
	(_ (close fin))
	(let c (+ a b))
	(_ (putStrLn (itoa c)))
	(fout (open fileName "2.txt") 1)
	(_ (putStrLnF fout (itoa c)))
	(_ (close fout))
|#
	(exit 0)
)
